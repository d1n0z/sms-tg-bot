[project]
name = "sms-tg-bot"
version = "0.1.0"
description = "Telegram bot that mirrors a HeroSMS-like one-time activation flow"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiogram>=3.28.2",
    "aiohttp>=3.13.0",
    "pydantic-settings>=2.14.1",
]
